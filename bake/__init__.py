"""Mesh baker: instantiate a .cgb document into glTF/GLB/OBJ meshes.

Import the functions from :mod:`bake.baker` directly, e.g.::

    from bake.baker import bake_scene, bake_file, primitive_to_mesh

(``bake.baker`` is intentionally not imported here so that
``python -m bake.baker`` runs without a runpy double-import warning.)
"""
