# Core dependencies for the .cgb format, validation, and the mesh baker.
# For the image-recognition pipeline (Phase 4-5) install requirements-recognition.txt.
jsonschema>=4.0
numpy>=1.23
scipy>=1.10
trimesh>=4.0
manifold3d>=2.3  # robust boolean (CSG) backend for baking `operations`
pygltflib>=1.16
